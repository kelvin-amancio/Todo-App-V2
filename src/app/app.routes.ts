import { Routes } from '@angular/router';
import { ListComponent } from './modules/todo-list/pages/list/list.component';

export const routes: Routes = [
    {
        path: "",
        component: ListComponent
    }
];
